name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Get-Greeting tests
        shell: pwsh
        run: .\tests\Test-Greeting.ps1
